version: '3.9'
services:
  app:
    build: .
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    image: app:django
    container_name: test_django
    command: python manage.py runserver 0.0.0.0:8000
